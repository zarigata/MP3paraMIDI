# Example environment configuration for MP3paraMIDI
# Copy this file to .env and adjust values for your environment.
# Never commit .env files containing secrets to version control.

# Flask Configuration
# Used only when running Flask directly via `python src/app.py`
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False

# ============================================================================
# UNIFIED SERVER (src/main.py)
# ============================================================================
APP_HOST=0.0.0.0
APP_PORT=8000

# Flask web UI: http://localhost:8000/
# Flask API: http://localhost:8000/api/
# Gradio Interface: http://localhost:8000/gradio

# ============================================================================
# GRADIO CONFIGURATION
# ============================================================================
GRADIO_SHARE=False
GRADIO_AUTH=
# Example with auth: GRADIO_AUTH=admin:secret123

# GRADIO_SHARE only works when running src/gradio_standalone.py.
# For public URLs with the unified server, use ngrok as documented in README.

# ============================================================================
# GRADIO STANDALONE MODE (src/gradio_standalone.py)
# ============================================================================
GRADIO_HOST=0.0.0.0
GRADIO_PORT=7860

# Storage Configuration
APP_STORAGE_ROOT=storage
RETAIN_UPLOADS=True
RETAIN_STEMS=True
ABSOLUTE_URLS=False

# Upload Limits (in megabytes)
MAX_UPLOAD_SIZE_MB=100

# CORS Configuration
# Comma-separated list of allowed origins.
# Use '*' for development only; specify exact origins for production.
CORS_ORIGINS=*

# Example for production:
# CORS_ORIGINS=https://example.com,https://app.example.com
